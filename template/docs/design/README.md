# Design

How each part of {{project-name}} works, and why it was built that way. Written
for someone arriving with no context: the code says *what*, these say *why*, and
what was rejected on the way.

One file per domain. A change that settles a decision updates that domain's file
in the same commit.

| Domain | Covers |
| --- | --- |
| _(add one per domain)_ | |
